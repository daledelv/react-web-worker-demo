import React from 'react';var Worker = require('worker!./Worker.js');export default class WorkerClient extends React.Component {    componentDidMount() {        // start up web worker        var worker = new Worker();        worker.addEventListener('message', (event) => {            this.updateCanvas(event.data);        });        worker.postMessage('message passed through');    }    updateCanvas(message) {        const ctx = this.refs.canvas.getContext('2d');        ctx.font = '24px sans-serif';        ctx.fillText(message, 10, 50);    }    render() {        return (            <canvas ref="canvas" width={300} height={100}/>        );    }}