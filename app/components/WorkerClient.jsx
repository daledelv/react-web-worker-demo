import React from 'react';export default class WorkerClient extends React.Component {    componentDidMount() {        // start up web worker        this.updateCanvas();    }    updateCanvas() {        const ctx = this.refs.canvas.getContext('2d');        ctx.font = '24px sans-serif';        ctx.fillText('Hello world placeholder', 10, 50);    }    render() {        return (            <canvas ref="canvas" width={300} height={100}/>        );    }}